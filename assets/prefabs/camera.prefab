-- Prefab de la camera
camera = {}
-- Tranform de la camera
GameObject.SetPosition(this, 1.0, 1.0, 0.0)
-- Composants de la camera
camera['camera'] = GameObject.AddComponent(this, "Camera")
camera['rts-controller'] = GameObject.AddComponent(this, "CameraRTSController")
